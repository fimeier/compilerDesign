// test divison and expressions in write.
//use lots of registers to see whether the division restores them correctly
//all regs in use and correct result.
class Main {
	void main() {
		int r1, r2, r3;
		r1 = 8;
		r2 = 2;
		r3 = 9;
		write(r1/r2/r2);
		writeln();
		write(r3/r2);
		writeln();
		r2 = r1*r3;
		write(r2);
		writeln();
		write(((3*4)/(2*3)) - ((4 + 7) + (9 + 13)));
	        writeln();
		write(((((3*4)/(2*3)) / ((18/4) / (13/9))) / (((17*12)/(2*3)) / ((18/4) / (13/9))))/((((700*4)/(2*3)) / ((18/4) / (13/9))) / (((17*12)/(2*3)) / ((18/4) / (13/9)))));
	}
}
