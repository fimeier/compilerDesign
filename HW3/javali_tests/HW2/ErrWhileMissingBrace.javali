class Main {
   void main() {
      int i;
    
      // Note: In Javali, while() loops must have braces ({}) after them,
      // so this is an expected syntax error.
      while(true){ //updated: added braces => error in the parsing stage...
         i = 1;
         }
  }
}
