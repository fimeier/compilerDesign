// Equality comparison on different (related through subtyping) classes is allowed
class Main{
	boolean b;
	void main(){
		A aref;
		B bref;
		aref = new A();
		bref = new B();
		b = aref == bref;
	}
}

class A {}

class B extends A {}