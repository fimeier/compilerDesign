class Main {
    void main() {
        a = foo(false || true, true && false);
        write(a);
    }

    int foo(boolean a, boolean b) {
        if (a && b) {
            return 1;
        } else {
            return 0;;
        }
    }
}
