// equality operators must take args of types L and R where either L is a subtype
// of R, or R is a subtype of L.

class Main {
    void main() {
        if (5 == true) {
            write(5);
            writeln();
        }
    }
}